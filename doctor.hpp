#include<iostream>
#include <queue>
#include "patients.hpp"

class Doctor
{
  private:
  std::string specialization;
  queue<Patients>;
  vector<int>; // This vector will contain the days of the month for which the doctor has available appointments

  public:
  Doctor(std::string specialization);

}
